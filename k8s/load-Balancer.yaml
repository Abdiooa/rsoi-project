apiVersion: v1
kind: Service
metadata:
  name: load-balancer
spec:
  selector:
    app: gateway
  type: LoadBalancer  
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30000
